function fn(str, ...express) {
    // ... 剩余操作符
    console.log(arguments)
}
let a = 1;
let b = 2;
// 注意传参方式，没有使用括号
fn `${a} + ${b} = ${a+b}`;
// 返回数组
// 0 - ["", " + ", " = ", ""]
// 用${xx}即插值将参数分割，三个插值，即第一数组，是用插值切割后的其他部分，长度为4
// 1 - 1，即${a}的值
// 2 - 2，即${b}的值
// 3 - 3，即${a + b}的值